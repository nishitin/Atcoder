function Main(input) {
    input = input.split("\n");
    let [H, N] = parseInt(input[0], 10);//モンスターの体力
    let A = parseInt(input[1], 10);//減らせる体力
    ans = 0;
    for (let i = 0; i < N; i++) {
        ans += A
    }
}
Main(require("fs").readFileSync("/dev/stdin", "utf8"));