function Main(input) {
    input = input.trim(" ").split("\n");
    let [N, M] = parseInt(input[0], 10);
    let = parseInt(input[1], 10);//夏休みの宿題
    let sum = 0
}
Main(require("fs").readFileSync("/dev/stdin", "utf8"));